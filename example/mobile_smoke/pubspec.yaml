name: opencode_auth_mobile_smoke
description: Device-only conformance fixture for opencode_auth.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  http: 1.6.0
  opencode_auth:
    path: ../..

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: false
  assets:
    - assets/localhost-cert.pem
    - assets/localhost-key.pem
